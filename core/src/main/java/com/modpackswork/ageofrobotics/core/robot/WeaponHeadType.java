package com.modpackswork.ageofrobotics.core.robot;

/** The weapon-heads a robot's hands can be fitted with (design doc §4.3). */
public enum WeaponHeadType {
  /** Balanced melee weapon. */
  SWORD,
  /** Fast, low-damage weapon suited to hunter behaviour. */
  HUNTING_CLAW
}
