package com.modpackswork.ageofrobotics.core.robot;

/** The tool-heads a robot's hands can be fitted with (design doc §4.3). */
public enum ToolHeadType {
  /** Mining jobs. */
  PICKAXE,
  /** Farming jobs. */
  HOE,
  /** Woodcutting jobs. */
  AXE,
  /** Building and demolition jobs. */
  HAMMER
}
